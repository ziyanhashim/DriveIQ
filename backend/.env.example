# MongoDB Atlas connection string (get this from the team)
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/driver_behavior?retryWrites=true&w=majority
MONGO_DB=driver_behavior

# JWT authentication
JWT_SECRET=change-me-to-any-random-string
JWT_ALG=HS256
JWT_EXPIRE_MIN=120
